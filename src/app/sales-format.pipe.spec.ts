import { SalesFormatPipe } from './sales-format.pipe';

describe('SalesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SalesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
